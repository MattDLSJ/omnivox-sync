playwright>=1.47
portalocker>=2.8
PyYAML>=6.0
python-dotenv>=1.0
pytest>=8.0
img2pdf>=0.5
# Python 3.14 on Windows ships no IANA database, so every zoneinfo lookup
# raises ZoneInfoNotFoundError: 'No time zone found with key America/Toronto'.
# Found on a real Windows 11 install; it stops the setup dead.
tzdata; sys_platform == 'win32'
